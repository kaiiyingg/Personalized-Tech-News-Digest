bcrypt==4.3.0
blinker==1.9.0
click==8.2.1
colorama==0.4.6
Flask==3.1.1
Flask-Bcrypt==1.0.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.2
python-dotenv==1.1.0
Werkzeug==3.1.3
feedparser==6.0.11
requests==2.28.1
pyotp==2.9.0

# Core dependencies for keyword-based classification (no ML needed)
beautifulsoup4==4.12.0

# Production dependencies optimized for Render free tier
gunicorn==21.2.0  # Production WSGI server
psycopg2-binary==2.9.10  # PostgreSQL adapter

# Optional ML dependencies (commented out for memory constraints)
# numpy>=1.24.0  # Uncomment if ML features are enabled
# torch>=2.0.0  # Uncomment if ML features are enabled  
# transformers>=4.30.0  # Uncomment if ML features are enabled
